{
    "version": 2,
    "builds": [
        {
            "src": "/server",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [
        {
            "source": "/server/(.*)",
            "destination": "/server"
        }
    ]
}